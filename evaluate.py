#Pour évaluer un auto-encodeur il faut enlever la partie espace latent/décodeur, poser un classifieur (couche dense ou autre) et vérifier sur un
#ensemble de données annotées si le classifieur est capable de prédire la classe de l'image.

